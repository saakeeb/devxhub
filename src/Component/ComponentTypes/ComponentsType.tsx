export type MorphingTextProps = {
  textList: string[];
};

export type ImageElementProps = {
  imageDesk: string;
  imageMob: string;
  text: string;
};

export type ElementDataTypes = {
  link: string;
  text: string;
  _id: string;
};

export type Element4thContactTypes = {
  icon: JSX.Element;
  text: string;
  link: string;
  _id: string;
};

export type Element4thSocialTypes = {
  icon: string;
  link: string;
  alt: string;
  _id: string;
};
